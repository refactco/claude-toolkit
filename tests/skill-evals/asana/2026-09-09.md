# Asana workflow evaluation — 2026-09-09

Task: `asana#1217577281347968`. Target: base pack 1.11.0.

## Independent checks

An independent agent read the skill and wrote 30 CLI workflow cases in
`tests/asana-workflow.test.mjs`. The helper was exercised as a child process
against a local API with synthetic credentials. A fetch guard blocked remote
network calls. Assertions inspect saved files, request bodies, and remote state.

The tests found two issues before release: `processed: false` was lost when a
task became a completed stub, and a short stale follower read stopped a requested
mention. Both were fixed. The final run passed all 30 cases.

Coverage includes pagination, direct child instructions, exact story/parent
matching, attachment bytes and redirects, no token on download requests, write
previews, author attribution, real mentions, delayed follower membership, task
completion, uncertain write responses, cache ownership, and bounded failures.
The 1Password process timeout was checked at about 20 seconds.

## Repository checks

- `node --test tests/*.test.mjs`: 64 passed, no failures or skipped tests.
- `node scripts/sync-codex.mjs --check`: 9 packs current.
- `python scripts/check-plugins.py`: 9 packs and 43 skills valid.
- `git diff --check`: passed.

The generic skill creator validator rejects this repository's custom metadata.
The repository requires that metadata, so it was preserved and checked with the
repository's validator. The generic validator is not counted as a pass.

## Live API checks

The configured RefactAgent account read the real task and an exact existing
story. A separate private, bot-owned parent and child were used for six live
checks: custom cache and child notes; attachment bytes; completion preview;
attributed multiline mention and exact story readback; parent-only completion;
and repeat completion. All six passed. The successful test parent and child
were then complete. No person received a test mention.

An earlier fixture setup created an unassigned task that the same bot could not
read afterward (403). That task could not be cleaned up through this account.
The successful fixture explicitly used the bot as owner and follower.

## Boundaries

- Saved mention markup is verified. Recipient inbox/email delivery is not
  observable through the API and was not tested on a human recipient.
- Subtask notes cover direct children. Child discussions and grandchildren
  require separate reads.
- Provider-only attachments and downloads over 100 MiB report incomplete work.
- CRE Daily main and stage contained no tracked Asana fork. Its project
  instructions use the marketplace; no client application code changed.
- Installed-copy and fresh-runtime checks must follow publication. The source
  checks above do not claim that another person's installation has updated.
